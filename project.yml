name: Peelr
options:
  bundleIdPrefix: com.performave
  deploymentTarget:
    macOS: "14.0"
  createIntermediateGroups: true

settings:
  base:
    SWIFT_VERSION: "5.0"
    MARKETING_VERSION: "1.0"
    CURRENT_PROJECT_VERSION: "1"
    DEVELOPMENT_TEAM: ""
    CODE_SIGN_STYLE: Automatic
    CODE_SIGN_IDENTITY: "-"
    ENABLE_HARDENED_RUNTIME: NO
    SWIFT_EMIT_LOC_STRINGS: YES

targets:
  Peelr:
    type: application
    platform: macOS
    sources:
      - path: Sources
    resources:
      - path: Resources
        optional: true
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.performave.peelr
        PRODUCT_NAME: Peelr
        INFOPLIST_KEY_CFBundleDisplayName: Peelr
        INFOPLIST_KEY_LSUIElement: YES
        INFOPLIST_KEY_NSHumanReadableCopyright: ""
        GENERATE_INFOPLIST_FILE: YES
        INFOPLIST_FILE: Info.plist
        ENABLE_APP_INTENTS: YES
        ASSETCATALOG_COMPILER_GENERATE_ASSET_SYMBOLS: NO
    dependencies:
      - sdk: Vision.framework
      - sdk: VisionKit.framework
      - sdk: CoreImage.framework
      - sdk: AppIntents.framework
      - sdk: Carbon.framework
      - sdk: AppKit.framework
      - sdk: CoreML.framework

  PeelrTests:
    type: bundle.unit-test
    platform: macOS
    sources:
      - path: Tests
      - path: Sources/Engine/ImageCompositing.swift
      - path: Sources/Engine/ColorKeyMatter.swift
      - path: Sources/Engine/ModeDetector.swift
      - path: Sources/Engine/Matter.swift
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.performave.peelr.tests
        GENERATE_INFOPLIST_FILE: YES
    dependencies:
      - sdk: AppKit.framework
      - sdk: CoreImage.framework

schemes:
  Peelr:
    build:
      targets:
        Peelr: all
    test:
      targets:
        - PeelrTests
